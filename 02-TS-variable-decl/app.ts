/*
    1.each variable name should start with lower case on the other hand classes,
     interfaces and enum starts with uppercase.
    2.Camelcase naming convention has to be followed.
*/

let age:number = 10;